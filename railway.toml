[build]
builder = "DOCKERFILE"
dockerfilePath = "vector-store/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 600
startCommand = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
